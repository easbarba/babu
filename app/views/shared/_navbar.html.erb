<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <%= link_to "Babu", root_path, id: "logo", class: 'navbar-brand' %>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <%= link_to 'Estórias', posts_path, class: "nav-link" %>
                </li>
            </ul>
            <form class="d-flex" role="search">
                <input class="form-control me-2" type="search" placeholder="Palavra" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Procurar</button>
            </form>
            <% if user_signed_in? %>
                <%= link_to current_user.name, destroy_user_session_path, method: :delete, class: "btn btn-warning"  %>
            <% else %>
                <%#= link_to 'Registar', new_user_registration_path, class: "btn btn-outline-light me-2" %>
                <%= link_to 'Entrar', new_user_session_path, class: "btn btn-warning" %>
            <% end %>
        </div>
    </div>
</nav>
